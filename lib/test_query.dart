import 'package:supabase_flutter/supabase_flutter.dart';
import 'package:flutter/material.dart';

void main() async {
  // Test script for querying chat rooms. Can't run this as a standalone dart file without supabase init.
}
